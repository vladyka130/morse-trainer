# 🧪 Локальне тестування Тренажера азбуки Морзе

## Швидкий старт (БЕЗ Apache)

Flet вже є веб-сервером, тому Apache не потрібен! Просто запустіть:

```bash
python main.py
```

Додаток автоматично:
- ✅ Запустить веб-сервер на `http://localhost:8550`
- ✅ Відкриє браузер автоматично
- ✅ Покаже сторінку авторизації

---

## Варіанти запуску

### Варіант 1: Стандартний запуск (рекомендовано)
```bash
python main.py
```
- Порт: `8550` (за замовчуванням)
- Доступ: `http://localhost:8550`
- Автоматично відкриває браузер

### Варіант 2: Запуск на іншому порту
```bash
python main.py
```
Або змініть в коді:
```python
port = int(os.environ.get("PORT", 8080))  # Змініть 8550 на 8080
```

### Варіант 3: Запуск через Flet CLI
```bash
flet run main.py --port 8550 --web
```

### Варіант 4: Запуск без автоматичного відкриття браузера
Змініть в `main.py`:
```python
ft.app(
    target=main,
    view=ft.WEB_BROWSER,  # Змініть на ft.AppView.FLET_APP для десктопного додатку
    port=port,
    host="127.0.0.1"  # Тільки локальний доступ
)
```

---

## Доступ з інших пристроїв у локальній мережі

Якщо хочете тестувати з телефону/планшета в тій же мережі:

1. **Дізнайтеся IP адресу комп'ютера**:
   - Windows: `ipconfig` (шукайте IPv4 адресу)
   - Linux/Mac: `ifconfig` або `ip addr`

2. **Змініть host в main.py**:
   ```python
   host="0.0.0.0"  # Слухати на всіх інтерфейсах
   ```

3. **Запустіть додаток**:
   ```bash
   python main.py
   ```

4. **Відкрийте на телефоні**:
   ```
   http://192.168.1.XXX:8550
   ```
   (замініть XXX на IP вашого комп'ютера)

---

## Якщо все ж потрібен Apache (необов'язково)

Apache може бути корисним як **reverse proxy** для:
- Використання стандартного порту 80/443
- Налаштування SSL сертифікатів
- Додаткового кешування

### Налаштування Apache як Reverse Proxy

1. **Встановіть Apache** (якщо ще не встановлено):
   - Windows: [XAMPP](https://www.apachefriends.org/) або [WAMP](https://www.wampserver.com/)
   - Linux: `sudo apt install apache2` (Ubuntu/Debian)

2. **Включіть модулі**:
   ```bash
   sudo a2enmod proxy
   sudo a2enmod proxy_http
   sudo a2enmod rewrite
   ```

3. **Створіть конфігурацію** `/etc/apache2/sites-available/morse-trainer.conf`:
   ```apache
   <VirtualHost *:80>
       ServerName morse-trainer.local
       
       ProxyPreserveHost On
       ProxyPass / http://localhost:8550/
       ProxyPassReverse / http://localhost:8550/
       
       # Для WebSocket (якщо потрібно)
       RewriteEngine on
       RewriteCond %{HTTP:Upgrade} websocket [NC]
       RewriteCond %{HTTP:Connection} upgrade [NC]
       RewriteRule ^/?(.*) "ws://localhost:8550/$1" [P,L]
   </VirtualHost>
   ```

4. **Активуйте сайт**:
   ```bash
   sudo a2ensite morse-trainer.conf
   sudo systemctl restart apache2
   ```

5. **Додайте в hosts** (Windows: `C:\Windows\System32\drivers\etc\hosts`):
   ```
   127.0.0.1 morse-trainer.local
   ```

6. **Відкрийте**: `http://morse-trainer.local`

**Але це НЕ обов'язково!** Flet працює відмінно і без Apache.

---

## Перевірка роботи

1. **Запустіть додаток**:
   ```bash
   python main.py
   ```

2. **Перевірте в браузері**:
   - Відкрийте `http://localhost:8550`
   - Має з'явитися сторінка авторизації
   - Спробуйте зареєструватися та увійти

3. **Перевірте логи**:
   - Логи зберігаються в `morse_trainer.log`
   - Консоль також показує логи

---

## Розв'язання проблем

### Порт зайнятий
Якщо порт 8550 зайнятий:
```bash
# Windows
netstat -ano | findstr :8550
# Linux/Mac
lsof -i :8550
```

Змініть порт в `main.py` або закрийте програму, яка використовує порт.

### Додаток не відкривається
- Перевірте, чи всі залежності встановлені: `pip install -r requirements.txt`
- Перевірте логи в `morse_trainer.log`
- Переконайтеся, що файл `morse_data.json` присутній

### Помилки з базою даних
- База даних створюється автоматично
- Переконайтеся, що є права на запис у поточній директорії

---

## Тестування перед розгортанням

Перед завантаженням на хостинг перевірте:

- ✅ Додаток запускається без помилок
- ✅ Реєстрація працює
- ✅ Логін працює
- ✅ Всі режими тренування працюють
- ✅ Звук відтворюється
- ✅ Статистика зберігається
- ✅ Адмін панель працює (для адміна)

---

## Підсумок

**Для локального тестування Apache НЕ потрібен!**

Просто запустіть:
```bash
python main.py
```

І відкрийте `http://localhost:8550` в браузері.

Apache може знадобитися тільки якщо:
- Потрібен стандартний порт 80/443
- Потрібен SSL сертифікат
- Потрібна додаткова конфігурація

Але для початкового тестування це зайве! 🚀

