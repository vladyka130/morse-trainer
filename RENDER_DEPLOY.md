# 🚀 Покрокова інструкція: Розгортання на Render.com

## Крок 1: Підготовка проекту

### 1.1 Переконайся, що всі файли готові:

Перевір наявність файлів:
- ✅ `main.py` - головний файл
- ✅ `morse_data.json` - дані символів (ОБОВ'ЯЗКОВО!)
- ✅ `requirements.txt` - залежності
- ✅ `.gitignore` - ігнорування файлів

### 1.2 Перевір `requirements.txt`:

Файл має містити:
```txt
flet>=0.21.0
numpy>=1.24.0
scipy>=1.10.0
playwright>=1.40.0
```

### 1.3 Перевір, що `main.py` налаштований для хостингу:

У кінці файлу має бути:
```python
if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 8550))
    ft.app(
        target=main,
        view=ft.WEB_BROWSER,
        port=port,
        host="0.0.0.0"
    )
```

✅ Це вже налаштовано в твоєму проекті!

---

## Крок 2: Створення GitHub репозиторію

### 2.1 Якщо ще немає GitHub акаунту:

1. Зареєструйся на https://github.com
2. Підтверди email

### 2.2 Створи новий репозиторій:

1. Натисни **"+"** → **"New repository"**
2. Назва: `morse-trainer` (або інша)
3. Опис (опціонально): "Тренажер азбуки Морзе на Flet"
4. Вибери **Public** (безкоштовний план) або **Private**
5. **НЕ** додавай README, .gitignore, license (вони вже є)
6. Натисни **"Create repository"**

### 2.3 Завантаж код на GitHub:

**Варіант A: Через командний рядок (рекомендовано)**

Відкрий PowerShell/CMD/Terminal в папці проекту:

```bash
# Ініціалізуй git (якщо ще не зроблено)
git init

# Додай всі файли
git add .

# Зроби перший коміт
git commit -m "Initial commit: Morse Trainer"

# Додай віддалений репозиторій (заміни YOUR_USERNAME на свій GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/morse-trainer.git

# Завантаж код
git branch -M main
git push -u origin main
```

**Варіант B: Через GitHub Desktop**

1. Завантаж GitHub Desktop: https://desktop.github.com
2. Відкрий проект в GitHub Desktop
3. Натисни **"Publish repository"**
4. Вибери назву та видимість
5. Натисни **"Publish repository"**

**Варіант C: Через веб-інтерфейс GitHub**

1. На сторінці репозиторію натисни **"uploading an existing file"**
2. Перетягни всі файли проекту (крім `__pycache__`, `.db`, `.log`)
3. Натисни **"Commit changes"**

---

## Крок 3: Реєстрація на Render.com

### 3.1 Створи акаунт:

1. Перейди на https://render.com
2. Натисни **"Get Started for Free"**
3. Вибери **"Sign up with GitHub"** (найпростіше)
4. Дозволь Render доступ до GitHub репозиторіїв

### 3.2 Підтвердження email:

1. Перевір email
2. Натисни на посилання підтвердження

---

## Крок 4: Створення Web Service на Render

### 4.1 Створи новий сервіс:

1. На головній панелі Render натисни **"New +"**
2. Вибери **"Web Service"**

### 4.2 Підключи GitHub репозиторій:

1. Якщо бачиш список репозиторіїв:
   - Знайди свій `morse-trainer` (або іншу назву)
   - Натисни **"Connect"**

2. Якщо не бачиш репозиторій:
   - Натисни **"Configure account"** або **"Connect GitHub"**
   - Дозволь доступ до репозиторіїв
   - Повернись і вибери репозиторій

### 4.3 Налаштування сервісу:

Заповни форму:

**Name:**
```
morse-trainer
```
(або інша назва, яку хочеш)

**Region:**
```
Frankfurt (EU)  # Найближче до України
```
або
```
Oregon (US West)  # Якщо немає EU
```

**Branch:**
```
main
```
(або `master`, якщо використовуєш стару гілку)

**Root Directory:**
```
(залиши порожнім)
```

**Runtime:**
```
Python 3
```

**Build Command:**
```
pip install -r requirements.txt && playwright install
```

**Start Command:**
```
python main.py
```

**Instance Type:**
```
Free  # Безкоштовний план
```

### 4.4 Додаткові налаштування (опціонально):

**Environment Variables (Змінні середовища):**
- `PORT` - Render встановить автоматично, не потрібно додавати
- Можна додати інші змінні, якщо потрібно

**Health Check Path:**
```
(залиши порожнім)
```

### 4.5 Створи сервіс:

1. Перевір всі налаштування
2. Натисни **"Create Web Service"**

---

## Крок 5: Очікування розгортання

### 5.1 Процес розгортання:

Render почне автоматично:
1. ✅ Клонувати репозиторій з GitHub
2. ✅ Встановлювати залежності (`pip install -r requirements.txt`)
3. ✅ Встановлювати Playwright браузери (`playwright install`)
4. ✅ Запускати додаток (`python main.py`)

**Це може зайняти 5-10 хвилин** (перший раз довше)

### 5.2 Перевірка логів:

Під час розгортання ти побачиш логи:
- ✅ "Cloning repository..."
- ✅ "Installing dependencies..."
- ✅ "Building application..."
- ✅ "Starting application..."

**Якщо бачиш помилки:**
- Перевір логи детально
- Переконайся, що всі файли завантажені на GitHub
- Перевір, що `morse_data.json` присутній

---

## Крок 6: Перевірка роботи

### 6.1 Отримай URL:

Після успішного розгортання Render надасть URL:
```
https://morse-trainer.onrender.com
```
(або інша назва, яку ти вибрав)

### 6.2 Відкрий в браузері:

1. Скопіюй URL
2. Відкрий в браузері
3. Має з'явитися сторінка авторизації тренажера

### 6.3 Перевір функціональність:

- ✅ Сторінка авторизації відображається
- ✅ Можна зареєструватися
- ✅ Можна увійти
- ✅ Тренажер працює
- ✅ Звук відтворюється

---

## Крок 7: Налаштування (опціонально)

### 7.1 Автоматичне оновлення:

Render автоматично оновлює додаток при push до GitHub:
```bash
git add .
git commit -m "Update"
git push
```

### 7.2 Кастомний домен:

1. В налаштуваннях сервісу знайди **"Custom Domains"**
2. Додай свій домен
3. Налаштуй DNS записи (інструкції в Render)

### 7.3 Environment Variables:

Якщо потрібні додаткові змінні:
1. В налаштуваннях сервісу знайди **"Environment"**
2. Додай змінні (KEY=VALUE)
3. Перезапусти сервіс

---

## ⚠️ Важливі зауваження

### База даних SQLite:

⚠️ **УВАГА**: На Render.com файлова система **ефемерна** (тимчасова).

**Проблема:**
- Дані в SQLite можуть втрачатися при перезапуску
- При оновленні додатку дані можуть зникнути

**Рішення:**
1. **Для тестування** - SQLite підійде
2. **Для продакшену** - використовуй PostgreSQL (Render надає безкоштовно)

### Тимчасові файли:

- WAV файли створюються автоматично
- Вони видаляються після використання
- Не потрібно нічого налаштовувати

### "Сон" додатку:

⚠️ На **безкоштовному плані** Render:
- Додаток "засинає" після 15 хвилин неактивності
- Перший запит після "сну" може займати 30-60 секунд
- Це нормально для безкоштовного плану

**Рішення:**
- Використовуй платний план ($7/міс) для постійної роботи
- Або використовуй cron job для "прокидання" додатку

---

## 🔧 Розв'язання проблем

### Проблема 1: "Build failed"

**Причини:**
- Помилки в `requirements.txt`
- Відсутні файли (`morse_data.json`)
- Неправильний Build Command

**Рішення:**
1. Перевір логи Build
2. Переконайся, що `morse_data.json` в репозиторії
3. Перевір `requirements.txt`

### Проблема 2: "Application failed to start"

**Причини:**
- Помилки в коді
- Неправильний Start Command
- Порт не налаштований

**Рішення:**
1. Перевір логи Runtime
2. Переконайся, що `main.py` використовує `PORT` змінну
3. Перевір Start Command: `python main.py`

### Проблема 3: "404 Not Found"

**Причини:**
- Додаток ще розгортається
- Додаток "спить" (безкоштовний план)
- Неправильний URL

**Рішення:**
1. Зачекай 30-60 секунд (якщо додаток "спить")
2. Перевір URL в налаштуваннях сервісу
3. Перевір логи

### Проблема 4: "Module not found"

**Причини:**
- Відсутня залежність в `requirements.txt`
- Неправильна версія Python

**Рішення:**
1. Додай залежність в `requirements.txt`
2. Переконайся, що використовується Python 3.11+
3. Перезапусти розгортання

### Проблема 5: "Playwright browsers not installed"

**Причини:**
- Відсутній `playwright install` в Build Command

**Рішення:**
1. Перевір Build Command: `pip install -r requirements.txt && playwright install`
2. Перезапусти розгортання

---

## 📝 Чеклист перед розгортанням

Переконайся, що:

- ✅ Всі файли завантажені на GitHub
- ✅ `morse_data.json` присутній в репозиторії
- ✅ `requirements.txt` містить всі залежності
- ✅ `main.py` налаштований для хостингу (PORT, host="0.0.0.0")
- ✅ `.gitignore` ігнорує `.db`, `.log`, `__pycache__`
- ✅ Додаток працює локально (`python main.py`)

---

## 🎉 Після успішного розгортання

Твій додаток буде доступний за URL:
```
https://morse-trainer.onrender.com
```

**Що далі:**
1. Поділися посиланням з друзями
2. Тестуй функціональність
3. Оновлюй код через GitHub push
4. Моніторь логи для виявлення проблем

---

## 💡 Поради

1. **Використовуй GitHub** - для автоматичного деплою
2. **Перевіряй логи** - при виникненні проблем
3. **Тестуй локально** - перед push на GitHub
4. **Зберігай резервні копії** - бази даних та конфігурацій
5. **Моніторь використання** - безкоштовний план має обмеження

---

## 📞 Підтримка

Якщо виникли проблеми:

1. **Перевір логи** в Render Dashboard
2. **Перевір GitHub** - чи всі файли завантажені
3. **Тестуй локально** - чи працює `python main.py`
4. **Перевір документацію** Render: https://render.com/docs

---

**Успішного розгортання! 🚀**

Твій додаток буде доступний по всьому світу! 🌍

